# Enums.gd
extends Node

enum EnemyState { IDLE, JUMP, VISUAL, NOISE, DEAD, HIT, ATTACHED }

enum PickupItems { SANDWICH, PROTEIN_SHAKE }
# nyi
enum PlayerWeapon { DEFAULT, FLAMETHROWER, SHOTGUN }
enum PlayerThrowableWeapon { GRENADE}
